name: Property Check live source smoke

on:
  pull_request:
    branches: [main]
    paths:
      - "site/**"
      - ".github/workflows/property-check-live-smoke.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: property-check-live-smoke-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  live-browser-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "24"

      - name: Use preinstalled Chrome
        run: |
          which google-chrome
          google-chrome --version
          npm init -y
          npm install --no-save playwright-core@1.55.0

      - name: Build checks-first workspace
        run: |
          python3 site/build-static.py .pages-site
          node --check .pages-site/live-property.js
          node --check .pages-site/checks-workspace-v5.js
          node --check .pages-site/checks-workspace-map.js
          node --check .pages-site/checks-workspace-v5-semantics.js
          node --check .pages-site/checks-workspace-v5-shortlist.js
          node --check .pages-site/full-report-preview.js
          grep -Fq 'LC-UX-v0.5.0' .pages-site/checks-workspace-v5.js
          grep -Fq 'LC-MAP-v0.2.0' .pages-site/checks-workspace-map.js
          grep -Fq 'LC-WORKSPACE-SEMANTICS-v0.1.1' .pages-site/checks-workspace-v5-semantics.js
          grep -Fq 'LC-WORKSPACE-SHORTLIST-v0.1.0' .pages-site/checks-workspace-v5-shortlist.js
          grep -Fq 'src="./checks-workspace-v5.js"' .pages-site/index.html
          grep -Fq 'src="./checks-workspace-map.js"' .pages-site/index.html
          grep -Fq 'src="./checks-workspace-v5-semantics.js"' .pages-site/index.html
          grep -Fq 'src="./checks-workspace-v5-shortlist.js"' .pages-site/index.html
          grep -Fq 'href="./checks-workspace-v5.css"' .pages-site/index.html
          grep -Fq 'href="./checks-workspace-map.css"' .pages-site/index.html
          grep -Fq 'href="./checks-workspace-v5-responsive.css"' .pages-site/index.html
          grep -Fq 'href="./checks-workspace-v5-polish.css"' .pages-site/index.html
          grep -Fq 'href="./checks-workspace-v5-semantics.css"' .pages-site/index.html
          ! grep -Fq 'src="./enhancements.js"' .pages-site/index.html
          ! grep -Fq 'src="./decision-cockpit-v4.js"' .pages-site/index.html
          ! grep -Fq 'src="./decision-cockpit-v3.js"' .pages-site/index.html
          ! grep -Fq 'src="./ux-overhaul.js"' .pages-site/index.html
          ! grep -Fq 'src="./ux-simplification.js"' .pages-site/index.html
          ! grep -Fq 'leaflet' .pages-site/index.html

      - name: Start static site
        run: |
          python3 -m http.server 8000 --directory .pages-site > /tmp/property-check-http.log 2>&1 &
          echo $! > /tmp/property-check-http.pid
          for i in {1..30}; do
            curl --fail --silent http://127.0.0.1:8000/ >/dev/null && exit 0
            sleep 1
          done
          cat /tmp/property-check-http.log
          exit 1

      - name: Test checks-first workspace
        env:
          PROPERTY_CHECK_BASE_URL: http://127.0.0.1:8000/
          PROPERTY_CHECK_ARTIFACT_DIR: site/tests/artifacts
          CHROME_PATH: /usr/bin/google-chrome
        run: node site/tests/workspace-v5-smoke.mjs

      - name: Capture workspace summary views
        env:
          PROPERTY_CHECK_BASE_URL: http://127.0.0.1:8000/
          PROPERTY_CHECK_ARTIFACT_DIR: site/tests/artifacts
          CHROME_PATH: /usr/bin/google-chrome
        run: node site/tests/workspace-v5-visual.mjs

      - name: Test v5-owned saved-property comparison
        env:
          PROPERTY_CHECK_BASE_URL: http://127.0.0.1:8000/
          PROPERTY_CHECK_ARTIFACT_DIR: site/tests/artifacts
          CHROME_PATH: /usr/bin/google-chrome
        run: node site/tests/comparison-smoke.mjs

      - name: Test full-product preview
        env:
          PROPERTY_CHECK_BASE_URL: http://127.0.0.1:8000/
          PROPERTY_CHECK_ARTIFACT_DIR: site/tests/artifacts
          CHROME_PATH: /usr/bin/google-chrome
        run: node site/tests/full-preview-smoke.mjs

      - name: Upload browser evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: property-check-live-browser-evidence
          path: site/tests/artifacts
          if-no-files-found: ignore
